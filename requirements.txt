# Flask Backend Requirements for SMU Classroom Dashboard
# Raspberry Pi - Moodle API Proxy Server

# Core framework
Flask==3.0.2
Werkzeug==3.0.1

# CORS support for React frontend
flask-cors==4.0.0

# Firebase Admin SDK for authentication verification
firebase-admin==6.4.0

# HTTP client for Moodle API calls
requests==2.31.0

# Optional: Production server (recommended over Flask dev server)
gunicorn==21.2.0

# Optional: Environment variable management
python-dotenv==1.0.1
